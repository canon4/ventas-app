spring.application.name=DeliciasExpress
server.port=8080

# URL de conexion a MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/delicias_express?allowPublicKeyRetrieval=true&useSSL=false


# Usuario y contrasena
spring.datasource.username=root
spring.datasource.password=Diego2004f

# Driver JDBC
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Dialecto de MySQL
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Mostrar consultas SQL
spring.jpa.show-sql=true

# Crear/actualizar tablas automaticamente
spring.jpa.hibernate.ddl-auto=update